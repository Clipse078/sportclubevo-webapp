/**
 * Shared content width for /dashboard/vereine header + directory workspace.
 * Keeps PageHeader and ClubDirectorySearchableList on one aligned grid.
 */
export const VEREINE_PAGE_CONTENT_WIDTH_CLASS = "max-w-6xl";
