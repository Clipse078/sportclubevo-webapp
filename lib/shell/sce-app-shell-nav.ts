/** SCE-VISUAL-03 — top global navigation shell (no permanent desktop sidebar inset). */
export const SCE_APP_SHELL_GLOBAL_NAV_CLASS = "sce-app-shell-global-nav";

/** Sticky global header block (primary + contextual rows). */
export const SCE_GLOBAL_APP_HEADER_CLASS = "sce-global-app-header";

/** Fixed mobile bottom primary navigation. */
export const SCE_MOBILE_BOTTOM_NAV_CLASS = "sce-mobile-bottom-nav";

/** CSS variable consumed by layout padding for the mobile bottom bar. */
export const SCE_MOBILE_BOTTOM_NAV_HEIGHT_VAR = "--sce-mobile-bottom-nav-height";
