/**
 * PLANNING-UX-07R7 — canonical facility resource visual kind resolver (shared across planning domains).
 * Icons must derive from FacilityResource type/group metadata, never from display names.
 */

export {
  resolveFacilityResourceVisualKind,
  type FacilityResourceVisualKind,
} from "@/components/admin/shared/planning/FacilityResourceIdentity";
