# Optional local-development starting point.
# Configure only features you use. Canonical names/scopes/failure behavior:
# docs/deployment/environment-manifest.md
APP_ENV=local
APP_BASE_URL=http://localhost:3000
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=
DATABASE_URL=
DIRECT_URL=

# SFV / ClubCorner Integration (Slice 1)
# Server-only — never use NEXT_PUBLIC_ prefix.
# Add values to .env.local only when developing this integration. Never commit values.
SFV_TOKEN_URL=
SFV_APPLICATION_KEY=
SFV_APPLICATION_PASS=
SFV_CLUB_ID=

# Stripe billing (SCE-SUPERADMIN-BILLING-01C) — server-only read integration
# LOCAL / STAGE / acceptance: use TEST secret key (sk_test_…)
# PRODUCTION: use LIVE secret key (sk_live_…)
# Never reuse production live keys in STAGE, acceptance, or local.
STRIPE_SECRET_KEY=

# SFV automatic sync (SFV-MATCH-SYNC-HOTFIX-01, Phase B)
# Required for /api/cron/sfv-sync (Vercel Cron, see vercel.json "crons").
# Vercel automatically sends this value as `Authorization: Bearer <CRON_SECRET>`
# on scheduled invocations when this env var is set on the project. The route
# rejects every request when CRON_SECRET is not configured (fail closed).
# Server-only — never use NEXT_PUBLIC_ prefix.
CRON_SECRET=

# Infoboard Screen 2 — Weather (WEATHER-01)
# Active provider: MeteoSwiss Open Data (SwissMetNet VQHA80)
#
# MeteoSwiss Open Data is used without restriction under the Swiss OGD licence.
# Attribution "Quelle: MeteoSwiss" is rendered in the UI when data is shown.
# No API key or paid subscription is required for the active MeteoSwiss provider.
# Data is fetched from the official FSDI endpoint (data.geo.admin.ch).
# Station: BAS (Basel/Binningen), ≈3.8 km from Sportanlage Im Brüel, Allschwil.
# Source documentation: https://opendatadocs.meteoswiss.ch/
#
# No weather-specific environment variables are required for the MeteoSwiss provider.
# The endpoint URL and station code are hardcoded in the server-side provider.
#
# ── DORMANT: Open-Meteo legacy provider ──────────────────────────────────────
# Open-Meteo is preserved as a dormant fallback implementation.
# It cannot activate without WEATHER_API_KEY AND an explicit change to
# ACTIVE_PROVIDER in lib/weather/weather-service.ts.
# Do NOT set WEATHER_API_KEY for FC Allschwil (MeteoSwiss is active).
#
# To switch to Open-Meteo (future):
#   1. Subscribe at https://open-meteo.com/en/pricing (API Standard or higher).
#   2. Set WEATHER_API_KEY to the issued API key (server-side only, never commit).
#   3. Change ACTIVE_PROVIDER in lib/weather/weather-service.ts to "open-meteo".
#   4. Update the attribution in InfoboardScreen2.tsx to "Wetterdaten: Open-Meteo.com".
#
# WEATHER_API_KEY=

# USER-ADMIN-01 — Outbound email (Resend) for password reset
# Required for the "Passwort vergessen?" flow to deliver reset links.
# Provider: https://resend.com — free tier (3,000/month, 100/day).
#
# Setup:
#   1. Create an account at resend.com.
#   2. Add and verify your sending domain.
#   3. Create an API key and set it below.
#   4. Set EMAIL_FROM to a verified sender address.
#   5. Set APP_BASE_URL (or NEXTAUTH_URL) to the canonical application URL.
#
# Missing RESEND_API_KEY or EMAIL_FROM → email delivery fails internally
# (MailConfigurationError). The forgot-password endpoint stays opaque.
# Missing APP_BASE_URL/NEXTAUTH_URL → same: internal failure, opaque response.
#
RESEND_API_KEY=
# Optional: use a separate API key with Receiving access.
# If set, inbound webhook processing will prefer this key over RESEND_API_KEY.
RESEND_RECEIVING_API_KEY=
# Sender address — domain must be verified in the Resend dashboard.
EMAIL_FROM=

# COMM-02 — inbound email replies (Resend Receiving + Webhook)
#
# EMAIL_INBOUND_DOMAIN:
#   Domain configured for inbound receiving in Resend (MX records point to Resend).
#   Outbound reply-capable emails will set Reply-To as:
#     reply+<opaque-token>@${EMAIL_INBOUND_DOMAIN}
#
# RESEND_WEBHOOK_SECRET:
#   Signing secret (whsec_...) for your Resend webhook endpoint.
#
EMAIL_INBOUND_DOMAIN=
RESEND_WEBHOOK_SECRET=

# SCE-CANONICAL-PUBLISHING-01 — external tenant website cache revalidation
# JSON map keyed by tenant slug. Server-only — never commit secrets.
# See docs/public-website-cache-revalidation.md
PUBLIC_WEBSITE_REVALIDATION_CONFIG=
