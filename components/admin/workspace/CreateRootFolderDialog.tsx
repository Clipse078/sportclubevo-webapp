"use client";

import { CreateWorkspaceFolderDialog } from "./CreateWorkspaceFolderDialog";

/** Tree-header control for creating a root folder. */
export function CreateRootFolderDialog() {
  return <CreateWorkspaceFolderDialog mode="root" />;
}
