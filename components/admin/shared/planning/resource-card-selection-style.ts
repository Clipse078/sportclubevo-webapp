/**
 * PLANNING-HUB-02F2 — dark-first selected resource cards (Planning family).
 */

/** Selected pitch/dressing card: dark surface + SCE-primary border (not light fill). */
export const RESOURCE_CARD_SELECTED_CLASSES =
  "border-[var(--sce-primary)] bg-[var(--surface)] ring-1 ring-[var(--sce-primary)]/35 shadow-[inset_0_0_0_1px_rgba(255,255,255,0.04)]";

/** Selected summary strip above pickers. */
export const RESOURCE_CARD_SELECTED_SUMMARY_CLASSES =
  "rounded-lg border border-[var(--sce-primary)]/35 bg-[var(--surface)] px-3 py-2";

/** Dressing-room icon tile when selected. */
export const RESOURCE_CARD_SELECTED_ICON_TILE_CLASSES =
  "border-[var(--sce-primary)] bg-[var(--surface-2)] text-[var(--sce-primary)]";
